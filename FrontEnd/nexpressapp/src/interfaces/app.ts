import { ReactNode } from "react";

export interface App {
  id: number;
  name: string;
  dateofcreation: string;
  capital: string;
  createdAt: ReactNode;
  creationDate: ReactNode;
  updatedAt: ReactNode;
  userId: number;
}
